TAILSPACE REPOSITORY INDEX FIX

Problem:
Tachimanga could not decode the repository index because the generated source entry
included an unsupported `versionId` property.

Apply:
1. Extract this ZIP.
2. Copy scripts/make_index.py into the repository, replacing the existing file.
3. Commit and push.
4. Let the GitHub Actions workflow rebuild/publish the `repo` branch.
5. In Tachimanga add this exact URL:

https://raw.githubusercontent.com/reddj2/tailspace-tachimanga-extension/repo/index.min.json

Do not use the normal github.com repository page URL.
