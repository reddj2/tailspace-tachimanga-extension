TAILSPACE FIX 03

Fixes:
@Source class must derive from HttpSource

The old ParsedHttpSource superclass was rejected by the current KSP source generator.
This patch changes Tailspace to directly extend HttpSource and replaces the
ParsedHttpSource-only selector methods with the equivalent Response parsers.

Apply:
1. Extract this ZIP.
2. Copy its contents into your repository folder and replace existing files.
3. Commit: Convert Tailspace to HttpSource
4. Push origin.
5. Send the next Build APK error, or the success screen.
