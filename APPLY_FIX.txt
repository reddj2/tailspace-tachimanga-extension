TAILSPACE MODERN REWRITE

This completely replaces the legacy 1.4 implementation with a Keiyoushi 1.6 KeiSource extension.

Apply:
1. Extract this ZIP.
2. Copy everything inside it into your repository folder.
3. Replace all existing files when prompted.
4. Commit with:
   Replace legacy source with KeiSource rewrite
5. Push origin.
6. Send the next Build APK result.

Important:
This should be treated as a new implementation. A successful compile does not prove
Tailspace selectors and reader behavior are correct; those must be tested in Tachimanga.
